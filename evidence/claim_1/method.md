# Method

This baseline freezes the exact acceptance contract. The current root deliberately
returns `BLOCKED`; a later cumulative child must execute the official EBM code and
add a separate independent trace checker.

